{% extends "layout.twig" %}
{% block content %}
  {{ fn('the_post') }}

<h-grid> <div class="large-12">

  <section class="product">
    <header class="product-header">
      {% do action("woocommerce_before_single_product") %}
    </header>

    <h-grid>
      {# Gallery #}
      <figure class="product-figure large-4">
        {% do action("woocommerce_before_single_product_summary") %}
      </figure>

      {# Price & Buy button #}
      <aside class="product-purchase large-3">
        {% do action("woocommerce_after_single_product_summary") %}
      </aside>

      {# Description tab #}
      <article class="product-article large-5">
        {% do action("woocommerce_single_product_summary") %}
      </article>
    </h-grid>
  </section>

  {% do action("woocommerce_after_single_product") %}

  {# RELATED #}
  <section class="product-related">
    <h2>{{ __('Related Products', 'my') }} </h2>
    {% include "woo/_products.twig" with { products: related_products } %}
  </section>

</div> </h-grid>

{% endblock %}
